# 1. Forward all backend API network traffic to the serverless function first
/api/*  /.netlify/functions/server/:splat  200

# 2. Let React Router handle all other frontend page paths
/*      /index.html                        200